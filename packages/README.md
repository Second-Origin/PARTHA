# Shared Packages

This directory is reserved for future shared packages that need to be consumed by multiple apps.

Keep app-specific code inside `apps/frontend` or `apps/backend` until a stable cross-app contract exists.
